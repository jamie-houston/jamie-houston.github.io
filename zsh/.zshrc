plugins=(
        adb
        git
        gradle
        zsh-autosuggestions
        zsh-syntax-highlighting
        )


#######
# slow plugins (uncomment to use)
# run this to see timing
# for i in $(seq 1 10); do /usr/bin/time $SHELL -i -c exit; done
#######

ZSH_THEME="powerlevel10k/powerlevel10k" # set by `omz`